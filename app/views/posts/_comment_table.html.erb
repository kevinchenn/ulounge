<table class="comments">
	<% @comments = post.comments.sort_by(&:num_likes)%>
	<% @comments.each do |comment| %>
		<%= render :partial => "comments/comment", :locals => {:comment => comment} %>
	<% end %>
</table>